#include <bits/stdc++.h>
using namespace std;

const int max_n = 100;

int main() {
    int N;
    vector<pair<int, int>> coordinates (max_n);

    cin >> N;
    for (int i = 0; i < N; i++) {
        cin >> coordinates[i].first >> coordinates[i].second;
    }

    
}
